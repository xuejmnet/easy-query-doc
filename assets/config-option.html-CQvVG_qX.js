import{_ as t}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as i,b as a,o as s}from"./app-CtIiamlL.js";const o={};function n(r,e){return s(),i("div",null,e[0]||(e[0]=[a(`<h1 id="configurable-parameter-options" tabindex="-1"><a class="header-anchor" href="#configurable-parameter-options"><span>Configurable Parameter Options</span></a></h1><p>Before use, we hope users can first review the optional configuration items, which will help understand how to optimize</p><div class="hint-container caution"><p class="hint-container-title">Breaking Changes!!!</p><blockquote><p>The framework has made some configuration changes from v2-v3. Please refer to <a href="/easy-query-doc/en/v2-v3">v2 upgrade to v3</a><br> The framework has made some configuration changes from v2-v3. Please refer to <a href="/easy-query-doc/en/v2-v3">v2 upgrade to v3</a><br> The framework has made some configuration changes from v2-v3. Please refer to <a href="/easy-query-doc/en/v2-v3">v2 upgrade to v3</a></p></blockquote></div><h2 id="default-configuration-items" tabindex="-1"><a class="header-anchor" href="#default-configuration-items"><span>Default Configuration Items</span></a></h2><table><thead><tr><th>Configuration Name</th><th>Default Value</th><th>Description</th></tr></thead><tbody><tr><td>database</td><td><code>DatabaseEnum.DEFAULT</code></td><td>Defaults to SQL92-compliant syntax. If your database is within <code>easy-query</code>&#39;s supported database range, please select the correct database you&#39;re using</td></tr><tr><td>deleteThrow</td><td><code>true</code></td><td><code>easy-query</code> defaults to not allowing physical deletion for data safety. This doesn&#39;t mean delete operations can&#39;t be executed, but that delete statements can&#39;t be generated after executing delete. It&#39;s recommended to use logical deletion to avoid this. For example, <code>delete from t_user where uid=1</code> will become <code>update t_user set deleted=1 where uid=1</code> after using logical deletion. The framework implements this function by default when using logical deletion, users still use the <code>deletable</code> method to call and execute</td></tr><tr><td>nameConversion</td><td><code>underlined</code></td><td>Currently has two options, though users can also implement the <code>NameConversion</code> interface themselves. Currently available: <code>default</code>, <code>underlined</code>, <code>upper_underlined</code>, <code>lower_camel_case</code>, <code>upper_camel_case</code>. Enabling <code>default</code> means the default object-to-database mapping relationship is by property name. For example, property name <code>userAge</code> corresponds to database column <code>userAge</code>. <code>underlined</code> means using underscore - <code>userAge</code> corresponds to database column <code>user_age</code>. Of course, after global settings, you can also manually specify the corresponding column name on <code>@Column</code> <a href="/easy-query-doc/en/framework/mapping-db">Related content</a></td></tr><tr><td>insertStrategy</td><td><code>ONLY_NOT_NULL_COLUMNS</code></td><td><code>insert</code> command defaults to inserting non-null columns. If a table has <code>id</code> and <code>name</code>, when the <code>name</code> column is null, the generated SQL will not specify the <code>name</code> column, like <code>insert into t_user (id) values(?)</code>. If the <code>name</code> column is not null, the generated SQL will be <code>insert into t_user (id,name) values(?,?)</code>. If the inserted collection has some null columns and some non-null columns, calling batch will generate n SQLs, merging the same SQLs together. So choose according to your needs (the default configuration is very good)</td></tr><tr><td>updateStrategy</td><td><code>ALL_COLUMNS</code></td><td>Default update command generated statement will update all columns of the entire object, not judging whether it&#39;s null. If needed, you can set whether null, not null, or all columns update</td></tr><tr><td>insertBatchThreshold</td><td>1024</td><td>If insertable adds an object collection of 1024 or more at once, it will merge the same SQL to improve execution efficiency. The connection string needs to add <code>rewriteBatchedStatements=true</code> (mysql). Can manually use or disable by calling the batch method of insert or update, like greater than or equal to 3. Don&#39;t ask why not default <code>batch</code> because <code>batch</code> some JDBC drivers or databases don&#39;t return the correct affected row count</td></tr><tr><td>updateBatchThreshold</td><td>1024</td><td>If updatable adds an object collection of 1024 or more at once, it will merge the same SQL to improve execution efficiency. The connection string needs to add <code>rewriteBatchedStatements=true</code> (mysql). Can manually use or disable by calling the batch method of insert or update, like greater than or equal to 3. Don&#39;t ask why not default <code>batch</code> because <code>batch</code> some JDBC drivers or databases don&#39;t return the correct affected row count</td></tr><tr><td>logClass</td><td>-</td><td>Under <code>spring-boot</code>, defaults to <code>com.easy.query.sql.starter.logging.Slf4jImpl</code> implementation. If you&#39;re not using <code>spring-boot</code>, you can implement yourself or use console logging <code>LogFactory.useStdOutLogging()</code></td></tr><tr><td>queryLargeColumn</td><td><code>true</code></td><td>Defaults to still querying columns marked as <code>large</code> under <code>@Column</code>. If you don&#39;t want to query, it&#39;s recommended to set the corresponding column to <code>@UpdateIgnore</code> with <code>updateSetInTrackDiff = true</code> under the premise of setting it as <code>large</code>, to prevent unqueried results from also being updated to null after all-column update</td></tr><tr><td>printSql</td><td><code>true</code></td><td>Whether to print executed SQL. This is different from log because considering sometimes you may need to view SQL rather than output log. So if set to true, the executed SQL and execution results will be recorded in logs as <code>log.info()</code>. If you haven&#39;t set log, you still can&#39;t see the corresponding executed SQL</td></tr><tr><td>relationGroupSize</td><td>512</td><td>Maximum associated IDs supported per query for include&#39;s associated query. If exceeded, will be executed as two statements, like greater than or equal to 1. Can be set separately at <code>.configure(o-&gt;o.setGroupSize(20))</code></td></tr><tr><td>noVersionError</td><td>true</td><td>When an object has a version number and is an expression update, if the version number <code>withVersion</code> is not added, it will error. The corresponding version number must be set. If you don&#39;t want error, you can ignore it through <code>ignoreVersion</code></td></tr><tr><td>warningColumnMiss</td><td><code>true</code></td><td>When JDBC&#39;s resultSet&#39;s columnName cannot be mapped to entity property, will output log.warning. <code>true</code>: means warning. <code>false</code>: means no warning</td></tr><tr><td>sqlParameterPrint</td><td>DEFAULT</td><td>SQL parameter printing optional MYBATIS mode with one more space after comma</td></tr><tr><td>mapToBeanStrict</td><td>true</td><td>Whether JDBC result set mapping to bean uses property strict mode</td></tr><tr><td>defaultSchema</td><td>null</td><td>When entity&#39;s schema is empty and defaultSchema is not empty, use defaultSchema</td></tr><tr><td>resultSizeLimit</td><td>-1</td><td>Limit global data fetch to at most how many rows. Not effective when less than or equal to 0. If set to 100 and subsequent queries need to expand, can set separately at <code>.configure(o-&gt;o.setResultSizeLimit(100000))</code></td></tr><tr><td>mapKeyConversion</td><td>-</td><td>When using map to return results, how to handle resultSet converted to map keys. Defaults to supporting all uppercase, all lowercase, removing underscores, and can also keep underscores</td></tr><tr><td>printNavSql</td><td>true</td><td>Whether associated queries print secondary subquery SQL</td></tr><tr><td>propertyMode</td><td><code>PropertyModeEnum.SAME_AS_ENTITY</code></td><td>Indicates entity properties are lowercase first letter (to be compatible with lambda and lambdakt modes). There&#39;s also <code>PropertyModeEnum.SAME_AS_ENTITY</code>. If you&#39;re in entity mode, it&#39;s recommended to use this</td></tr><tr><td>relationTableAppend</td><td><code>RelationTableAppendEnum.SMART</code></td><td><code>SMART</code> means intelligently add <code>relationTable</code> implicit join. <code>DEFAULT</code> requires wrapping in <code>if</code> code block when code execution doesn&#39;t execute to navigation property</td></tr><tr><td>mappingStrategy</td><td><code>EntityMappingStrategyEnum.PROPERTY_FIRST</code></td><td>Mapping relationship between objects. Defaults to mapping by <code>column name</code>. Can choose <code>property name</code> or <code>column name + property name</code></td></tr><tr><td>includeLimitMode</td><td><code>IncludeLimitModeEnum.PARTITION</code></td><td>When fetching many sub-items, if sub-items have limit set to limit return count, defaults to <code>partition</code> to improve performance but some databases don&#39;t support it. MySQL5.7 users can choose <code>union_all</code> to support include+limit</td></tr><tr><td>saveComment</td><td><code>false</code></td><td><code>true</code>: then persist <code>@Column(comment=&quot;...&quot;)</code> and <code>@Table(comment=&quot;...&quot;)</code> annotations to EntityMetadata and ColumnMetadata</td></tr><tr><td>maxInClauseSize</td><td><code>9999999</code></td><td>When <code>.where(o -&gt; o.id().in(ids))</code> can form <code>(id in (:p1,:p2) or id in(:p3,:p4))</code> according to the set <code>maxInClauseSize</code></td></tr></tbody></table><h2 id="sharding-specific-configuration" tabindex="-1"><a class="header-anchor" href="#sharding-specific-configuration"><span>Sharding-Specific Configuration</span></a></h2><table><thead><tr><th>Configuration Name</th><th>Default Value</th><th>Description</th></tr></thead><tbody><tr><td>sharding</td><td><code>false</code></td><td>Whether to enable sharding. Not enabled by default. New configuration in 3.0.3^</td></tr><tr><td>connectionMode</td><td><code>SYSTEM_AUTO</code></td><td>By default, the framework changes the connection mode for sharding connections to automatic. The framework will handle it automatically without user specification. Of course, users can also specify the connection mode themselves. 1. <code>MEMORY_STRICTLY</code> memory strict mode, meaning if cross-table or cross-database queries exist, this query will strictly control memory, querying all tables at once as much as possible. For a single database, if querying all tables, each table needs a <code>connection</code>, so a single query may exhaust the connection pool or even be insufficient. So usually works with the configuration parameter <code>maxShardingQueryLimit</code> as a limit. 2. <code>CONNECTION_STRICTLY</code> connection count limit, still using <code>maxShardingQueryLimit</code> as the maximum connection count, using as few connections as possible to execute cross-shard query merging. Mainly affects aggregation mode after sharding, whether to use stream aggregation or memory aggregation. Generally, users don&#39;t need to set.</td></tr><tr><td>maxShardingQueryLimit❗️</td><td><code>5</code></td><td>Suppose a single query involves querying across 13 tables. Because the query doesn&#39;t have a sharding key, this query will group 13 tables under the same database into 5 per group, dividing into 3 groups with the last group having 3 tables. The current query will acquire 5 connections at once. These 5 connections will be limited by the <code>defaultDataSourceMergePoolSize</code> parameter, then returned to the <code>DataSource</code> connection pool after this query is completed. This parameter cannot be set larger than the <code>DataSource</code>&#39;s <code>pool-size</code>, otherwise it may cause the program to freeze, because if the connection pool is 20 and a single query needs 21, it will keep waiting until timeout and still can&#39;t get 21</td></tr><tr><td>defaultDataSourceMergePoolSize❗️</td><td><code>0</code></td><td>If any of your tables are sharded, you must set this value, and must be set less than or equal to the <code>DataSource</code> connection pool size. Suppose the connection pool size is 100, then this value can be set to 60, 70, 80, or even 100, but cannot be larger than the connection pool, and must be greater than or equal to <code>maxShardingQueryLimit</code>. If the connection pool is 100 and the current value is set to 10, it means all threads only have 10 connections within the connection pool that can be used for shard aggregation queries (10 per data source)</td></tr><tr><td>multiConnWaitTimeoutMillis</td><td><code>5000</code></td><td>By default, for shard connection acquisition operations greater than 1, deduct from the total of <code>defaultDataSourceMergePoolSize</code>. For example, with 100 connection pool and shard set to 10, if 3 threads all need 5 shard aggregations, one thread definitely can&#39;t acquire, so it will wait for the default 5 seconds. If after this time the first two still haven&#39;t finished querying, it will throw an error</td></tr><tr><td>warningBusy</td><td><code>true</code></td><td>During shard aggregation, because multiple connections need to be acquired at once, still using the above case, suppose the third thread acquired 5 connections but the acquisition time exceeded 80% of <code>multiConnWaitTimeoutMillis</code> time, the framework will print that acquiring connections is busy. You may need to readjust this value of <code>defaultDataSourceMergePoolSize</code> and adjust the connection pool size</td></tr><tr><td>maxShardingRouteCount</td><td><code>128</code></td><td>When conditional sharding exceeds how much to error, default 128. That is, for example, select where update where delete where routes to too many tables will error. For entity operations like update object, insert, delete object, this condition won&#39;t be judged</td></tr><tr><td>defaultDataSourceName</td><td><code>ds0</code></td><td>Default sharding datasource name. If you don&#39;t need database sharding, you don&#39;t need to set this value</td></tr><tr><td>shardingExecuteTimeoutMillis</td><td><code>60000</code></td><td>Shard aggregation timeout default 60 seconds in units (ms), including CRUD</td></tr><tr><td>throwIfRouteNotMatch</td><td><code>true</code></td><td>Whether to error when query doesn&#39;t match route. Default is if not choose to save, return default value. For example, sharding by time, if the starting shard table is January 2020, then if you query 2019 or query future time, the framework internally doesn&#39;t have this time yet, so the route obtained by this query is empty. You can choose not to error and return default value, like toList will be an empty collection, count will be 0, etc.</td></tr><tr><td>executorMaximumPoolSize</td><td><code>0</code></td><td>Maximum shard aggregation thread count. Default 0 will use <code>Executors.newCachedThreadPool</code> thread pool. If you need to set or customize, please set to at least maxShardingQueryLimit*shard count. After setting value, will use bounded queue thread pool</td></tr><tr><td>executorCorePoolSize</td><td><code>Math.min(processors, 4)</code></td><td>Only effective when <code>executorMaximumPoolSize</code>&gt;0. Where <code>processors</code> is <code>Runtime.getRuntime().availableProcessors()</code></td></tr><tr><td>executorQueueSize</td><td><code>1024</code></td><td>Only effective when <code>executorMaximumPoolSize</code>&gt;0. Thread pool bounded queue size</td></tr><tr><td>startTimeJob</td><td><code>false</code></td><td>When using the system default time-based sharding, setting this configuration to <code>true</code> will add corresponding system tables in memory. The principle is to start a scheduled task thread to execute</td></tr><tr><td>shardingFetchSize</td><td><code>1000</code></td><td>Default fetch size setting under sharding</td></tr><tr><td>shardingQueryInTransaction</td><td>serializable</td><td><code>serializable</code> queries within transactions will set maxShardingQueryLimit to 1 to go serial to prevent dirty reads because current transactions are not allowed under concurrent queries. You can also use <code>concurrency</code> to ignore current transaction queries. Performance-wise, <code>concurrency</code> is prioritized. <code>serializable</code> will ensure relative data consistency. You can also manually set queryable().useMaxShardingQueryLimit(n)</td></tr></tbody></table><h2 id="spring-boot" tabindex="-1"><a class="header-anchor" href="#spring-boot"><span>spring-boot</span></a></h2><p>Configuration file can directly configure the above options</p><div class="language-yml line-numbers-mode" data-highlighter="shiki" data-ext="yml" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code><span class="line"></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">easy-query</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">:</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  enable</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">true</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  name-conversion</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">underlined</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  database</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">mysql</span></span>
<span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">  #If executing physical delete statement will error. If changed to false, can later allow through allowDeleteStatement</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  delete-throw</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">true</span></span>
<span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">  #Print SQL display (requires framework to have default log to print with log.info)</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  print-sql</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">true</span></span>
<span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">  #Entity mapping to dto/vo uses property matching mode</span></span>
<span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">  #Supports property_only column_only column_and_property property_first</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;">  mapping-strategy</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">property_first</span></span>
<span class="line"><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">  ......</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="non-spring-boot" tabindex="-1"><a class="header-anchor" href="#non-spring-boot"><span>Non-spring-boot</span></a></h2><div class="language-java line-numbers-mode" data-highlighter="shiki" data-ext="java" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code><span class="line"><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;"> EasyQueryClient</span><span style="--shiki-light:#E45649;--shiki-dark:#E06C75;"> easyQueryClient </span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;">=</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;"> EasyQueryBootstrapper</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">defaultBuilderConfiguration</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">()</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">                .</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">setDefaultDataSource</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(dataSource)</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">                .</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">optionConfigure</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(op </span><span style="--shiki-light:#C18401;--shiki-dark:#C678DD;">-&gt;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> {</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">                    op</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">setDeleteThrowError</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">true</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">);</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">//Set to not allow physical deletion</span></span>
<span class="line"><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">                    op</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">setPrintSql</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">true</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">);</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">//Set to print executed SQL info in log.info mode</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">                    ......</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">//Here used to configure system default configuration options</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">                })</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">                .</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">replaceService</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">NameConversion</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">class</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">, </span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">UnderlinedNameConversion</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#E45649;--shiki-dark:#E5C07B;">class</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">)</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">//Replace framework internal property and column conversion mode to uppercase to underscore</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">                .</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">useDatabaseConfigure</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">new</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;"> MySQLDatabaseConfiguration</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">())</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">//Set dialect syntax etc. to MySQL&#39;s</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">                .</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">build</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">();</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,12)]))}const c=t(o,[["render",n]]),h=JSON.parse('{"path":"/en/framework/config-option.html","title":"Configuration Parameter Options (Important)❗️❗️❗️","lang":"en-US","frontmatter":{"title":"Configuration Parameter Options (Important)❗️❗️❗️","order":10,"description":"Configurable Parameter Options Before use, we hope users can first review the optional configuration items, which will help understand how to optimize Breaking Changes!!! The fr...","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"Configuration Parameter Options (Important)❗️❗️❗️\\",\\"image\\":[\\"\\"],\\"dateModified\\":\\"2025-10-27T05:04:42.000Z\\",\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"xuejmnet\\",\\"url\\":\\"https://github.com/xuejmnet\\"}]}"],["meta",{"property":"og:url","content":"https://github.com/dromara/easy-query/easy-query-doc/en/framework/config-option.html"}],["meta",{"property":"og:site_name","content":"Documentation"}],["meta",{"property":"og:title","content":"Configuration Parameter Options (Important)❗️❗️❗️"}],["meta",{"property":"og:description","content":"Configurable Parameter Options Before use, we hope users can first review the optional configuration items, which will help understand how to optimize Breaking Changes!!! The fr..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"en-US"}],["meta",{"property":"og:locale:alternate","content":"zh-CN"}],["meta",{"property":"og:updated_time","content":"2025-10-27T05:04:42.000Z"}],["meta",{"property":"article:modified_time","content":"2025-10-27T05:04:42.000Z"}],["link",{"rel":"alternate","hreflang":"zh-cn","href":"https://github.com/dromara/easy-query/easy-query-doc/framework/config-option.html"}]]},"git":{"createdTime":1761500387000,"updatedTime":1761541482000,"contributors":[{"name":"只是我","username":"","email":"alice@example.com","commits":2}]},"readingTime":{"minutes":6.94,"words":2083},"filePathRelative":"en/framework/config-option.md","autoDesc":true}');export{c as comp,h as data};
